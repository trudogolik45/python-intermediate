services:
  web_app:
    build:
      context: .
    container_name: fastapi_app
    command: fastapi dev src/main.py --host 0.0.0.0 --port 8000
    ports:
      - "8010:8000"
    volumes:
      - ./src:/app/src
